# Restore project 
npm install
# Build project
ng build
#Build product
ng build --prod --build-optimizer
#Run project
ng serve
#Deploy firebase
firebase deploy

#update packages
npm i -g npm-check-updates